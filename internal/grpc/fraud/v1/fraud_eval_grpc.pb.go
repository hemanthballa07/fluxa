// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             v7.35.0
// source: proto/fraud/v1/fraud_eval.proto

package fraudv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	FraudEval_EvaluateTransaction_FullMethodName = "/fluxa.fraud.v1.FraudEval/EvaluateTransaction"
)

// FraudEvalClient is the client API for FraudEval service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// FraudEval is the synchronous fraud evaluation surface.
// Async ingest via RabbitMQ remains the path of record for offline replay and
// analytics; this RPC exists for hot-path callers (bankops-portal) that need
// a decision before committing a transaction.
type FraudEvalClient interface {
	// EvaluateTransaction runs all configured rules against the transaction and
	// returns a decision plus the flags that fired. The caller decides what to
	// do with flagged transactions (HELD, DENIED, allowed-with-case, etc.) —
	// Fluxa is a detection service, not an enforcement engine.
	//
	// Latency SLO: p99 < 50ms with rules-only evaluation; <100ms with the ML
	// scorer enabled. The server returns latency_ms in the response so callers
	// can verify.
	EvaluateTransaction(ctx context.Context, in *EvaluateRequest, opts ...grpc.CallOption) (*EvaluateResponse, error)
}

type fraudEvalClient struct {
	cc grpc.ClientConnInterface
}

func NewFraudEvalClient(cc grpc.ClientConnInterface) FraudEvalClient {
	return &fraudEvalClient{cc}
}

func (c *fraudEvalClient) EvaluateTransaction(ctx context.Context, in *EvaluateRequest, opts ...grpc.CallOption) (*EvaluateResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EvaluateResponse)
	err := c.cc.Invoke(ctx, FraudEval_EvaluateTransaction_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// FraudEvalServer is the server API for FraudEval service.
// All implementations must embed UnimplementedFraudEvalServer
// for forward compatibility.
//
// FraudEval is the synchronous fraud evaluation surface.
// Async ingest via RabbitMQ remains the path of record for offline replay and
// analytics; this RPC exists for hot-path callers (bankops-portal) that need
// a decision before committing a transaction.
type FraudEvalServer interface {
	// EvaluateTransaction runs all configured rules against the transaction and
	// returns a decision plus the flags that fired. The caller decides what to
	// do with flagged transactions (HELD, DENIED, allowed-with-case, etc.) —
	// Fluxa is a detection service, not an enforcement engine.
	//
	// Latency SLO: p99 < 50ms with rules-only evaluation; <100ms with the ML
	// scorer enabled. The server returns latency_ms in the response so callers
	// can verify.
	EvaluateTransaction(context.Context, *EvaluateRequest) (*EvaluateResponse, error)
	mustEmbedUnimplementedFraudEvalServer()
}

// UnimplementedFraudEvalServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedFraudEvalServer struct{}

func (UnimplementedFraudEvalServer) EvaluateTransaction(context.Context, *EvaluateRequest) (*EvaluateResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method EvaluateTransaction not implemented")
}
func (UnimplementedFraudEvalServer) mustEmbedUnimplementedFraudEvalServer() {}
func (UnimplementedFraudEvalServer) testEmbeddedByValue()                   {}

// UnsafeFraudEvalServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to FraudEvalServer will
// result in compilation errors.
type UnsafeFraudEvalServer interface {
	mustEmbedUnimplementedFraudEvalServer()
}

func RegisterFraudEvalServer(s grpc.ServiceRegistrar, srv FraudEvalServer) {
	// If the following call pancis, it indicates UnimplementedFraudEvalServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&FraudEval_ServiceDesc, srv)
}

func _FraudEval_EvaluateTransaction_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EvaluateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(FraudEvalServer).EvaluateTransaction(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: FraudEval_EvaluateTransaction_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(FraudEvalServer).EvaluateTransaction(ctx, req.(*EvaluateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// FraudEval_ServiceDesc is the grpc.ServiceDesc for FraudEval service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var FraudEval_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "fluxa.fraud.v1.FraudEval",
	HandlerType: (*FraudEvalServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "EvaluateTransaction",
			Handler:    _FraudEval_EvaluateTransaction_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "proto/fraud/v1/fraud_eval.proto",
}
